import styled from 'styled-components';

const Footer = styled.div`
  position: fixed;
  bottom: 0;
  background: black;
  width: 100%;
  color: white;
  text-align: center;
  font-size: 12px;
  z-index: 10;
`;

const Styled = { Footer };
export default Styled;
