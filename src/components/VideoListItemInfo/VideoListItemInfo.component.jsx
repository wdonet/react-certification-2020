import React from 'react';

function VideoListIteminfo({ title, description }) {
  return (
    <div>
      <h3>{title}</h3>
      <p>{description}</p>
    </div>
  );
}

export default VideoListIteminfo;
