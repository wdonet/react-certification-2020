import styled from 'styled-components';

const Container = styled.div`
  border: 10px solid #cccccc;
  border-radius: 15px;
  padding: 20px;
  width: 300px;
  margin-bottom: 20px;
  margin: 10px auto;
`;

const Title = styled.h1``;

const Body = styled.p``;

const Preview = styled.img`
  width: 100%;
`;

const Styled = { Container, Title, Body, Preview };
export default Styled;
