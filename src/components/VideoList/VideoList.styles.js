import styled from 'styled-components';

const Container = styled.div``;

const Title = styled.h1`
  text-align: center
`;

const List = styled.div`
  padding: 0 15px;
  display: flex;
  flex-flow: row wrap;
`;

const Styled = { Container, Title, List };
export default Styled;
