import React from 'react';

const SearchBar = () => {
  return (
    <input
      className="header-bar"
      name="header_text"
      placeholder="🎥  Search for videos"
    />
  );
};

export default SearchBar;
