import React from 'react';
import logo from '../../../img/m-logo.jpg';

const Logo = () => {
  return (
    <div>
      <img src={logo} alt="Logo" height={90} />
    </div>
  );
};

export default Logo;
